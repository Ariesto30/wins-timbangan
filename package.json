{
  "name": "wins-timbangan",
  "version": "1.0.0",
  "description": "Aplikasi Manajemen Timbangan PT WINS Sawit",
  "private": true,
  "scripts": {
    "install:all": "cd backend && npm install && cd ../frontend && npm install",
    "build": "cd frontend && npm install && npm run build",
    "start": "cd backend && NODE_ENV=production node server.js",
    "render-build": "cd backend && npm install && cd ../frontend && npm install && npm run build",
    "render-start": "cd backend && NODE_ENV=production node server.js",
    "dev:backend": "cd backend && node server.js",
    "dev:frontend": "cd frontend && npm run dev -- --host"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
